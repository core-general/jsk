# Web Client Core Spring

Spring `@Configuration` that wires web client infrastructure beans.

## What It Solves

- `WebClientConfig` registers `WebAuthClientWithStaticSecrets` and `WebJavaSpecificHelper` beans

## Key Details

- Only 2 beans — thin wiring module
