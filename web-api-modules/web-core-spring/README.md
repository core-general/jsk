# Web Core Spring

Spring configuration for `web-core` — registers metadata beans.

## What It Solves

- `WebCoreConfig` creates `ApiClassUtil(IJson)` and `WebMethodInfoProviderImpl` beans

## Key Details

- Very thin (1 file) — bridges `web-core` into Spring's DI
