# JX Logging Logback

Logback configuration helper for JSK applications.

## What It Solves

- `JskLoggingLogback` provides programmatic Logback configuration (log levels, patterns, appenders)
- Ensures consistent logging setup across all JSK applications

## Key Details

- Small utility module
- Uses Logback as the SLF4J backend
