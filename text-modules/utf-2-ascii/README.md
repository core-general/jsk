# UTF to ASCII

Unicode-to-ASCII transliteration and URL slug generation.

## What It Solves

- `Uni2AsciiDecoder` converts Unicode text to ASCII equivalents (transliteration)
- URL-safe slug generation from arbitrary Unicode strings

## Key Details

- Useful for generating URL-friendly slugs from non-Latin text (Cyrillic, CJK, accented characters, etc.)
